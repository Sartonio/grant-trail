# === MANDATORY ===

# Supabase
SUPABASE_ACCESS_TOKEN=
SUPABASE_PROJECT_REF=
# DB connection password (dashboard → Project Settings → Database). Lets the
# CLI connect directly for `db push`/`db query` instead of provisioning a
# temporary login role (which needs privileges our identity lacks).
SUPABASE_DB_PASSWORD=

# Stripe
STRIPE_SECRET_KEY=
STRIPE_PRICE_BASIC=
STRIPE_PRICE_FISCAL_AGENT=
STRIPE_PRODUCT_BASIC=
STRIPE_PRODUCT_FISCAL_AGENT=
STRIPE_WEBHOOK_SECRET=        # whsec_… from the `stripe webhook_endpoints create` step (Stripe reveals it only at creation)

# Vercel
VERCEL_TOKEN=
VERCEL_ORG_ID=
VERCEL_PROJECT_ID=

# Email
RESEND_API_KEY=
EMAIL_FROM=GrantTrail <onboarding@resend.dev>

# App
APP_URL=

# === OPTIONAL ===

# Stripe
STRIPE_BILLING_PORTAL_CONFIGURATION_ID=

# Observability
VITE_SENTRY_DSN=

# === AUTOFILLED ===

# Supabase
VITE_SUPABASE_URL=
VITE_SUPABASE_KEY=
