name: Deploy to Production

# Production deploy. Manual trigger only (workflow_dispatch).
# Delegates the full deploy pipeline to the reusable deploy.yml.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yml
    with:
      environment: production
    secrets: inherit
