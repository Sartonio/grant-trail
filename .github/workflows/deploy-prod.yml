name: Deploy to Production

# Production deploy. Manual trigger only (workflow_dispatch).
# Delegates the full deploy pipeline to the reusable deploy.yml.
on:
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  # TEMPORARILY DISABLED (restore before relying on production safety):
  # the require-green-ci gate below blocks a prod deploy unless ci.yml passed
  # for this exact commit. Uncomment it AND re-add `needs: require-green-ci`
  # on the deploy job to re-enable.
  # require-green-ci:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Require a successful ci.yml run for this commit
  #       env:
  #         GH_TOKEN: ${{ github.token }}
  #         REPO: ${{ github.repository }}
  #         SHA: ${{ github.sha }}
  #       run: |
  #         set -euo pipefail
  #         count=$(gh api \
  #           "/repos/$REPO/actions/workflows/ci.yml/runs?head_sha=$SHA&status=completed" \
  #           --jq '[.workflow_runs[] | select(.conclusion == "success")] | length')
  #         if [ "${count:-0}" -eq 0 ]; then
  #           echo "::error::No successful ci.yml run found for $SHA. Refusing to deploy."
  #           exit 1
  #         fi
  #         echo "Found $count successful ci.yml run(s) for $SHA."

  deploy:
    uses: ./.github/workflows/deploy.yml
    with:
      environment: production
    secrets: inherit
