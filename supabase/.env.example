# This file is copied to supabase/.env by `npm run setup`.
# It is NOT read by the Supabase CLI or edge functions — it is a stub
# created for historical reasons. The actual edge function secrets live in
# supabase/functions/.env (copied from supabase/functions/.env.example).
#
# See docs/how_to/local_stripe_testing.md and docs/how_to/local_email_testing.md.
