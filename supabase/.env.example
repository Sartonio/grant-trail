# Local Edge Function secrets — copy to supabase/.env.
# Guide: docs/how_to/local_stripe_testing.md

# === MANDATORY ===

# Stripe (test mode)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_BASIC=
STRIPE_PRICE_FISCAL_AGENT=

# App
APP_URL=http://localhost:3000

# === OPTIONAL ===

# Email
SMTP_HOST=
SMTP_USER=
SMTP_PASS=
SMTP_PORT=465
SMTP_FROM=
