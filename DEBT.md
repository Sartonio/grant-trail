# DEBT.md — bug & limitation ledger

Any bug or limitation discovered but not fixed in a task gets an entry here, in
the **same commit** as the task. Entries are a running history: don't delete
them — flip `status` to `fixed` or `wontfix` instead.

**Format** (one entry per bug):

```
status | area | description | discovered-during: <context> (date)
```

`status` is one of `open` | `fixed` | `wontfix`.

## Entries

- open | frontend coupling | 14 component files import supabaseClient directly (allowed for auth/storage today but unenforced; boundary allowlist planned) | discovered-during: framework survey 2026-07-14
- open | validation divergence | return-path/origin validation exists only in supabase/functions/_shared/validation.ts with no frontend counterpart; frontend and edge functions can drift on what's accepted | discovered-during: framework survey 2026-07-14
- open | edge lint suppressed | `no-import-prefix` deno lint rule disabled in supabase/functions/deno.json — the edge functions import deps via inline `npm:` specifiers (Supabase Edge runtime pattern) with no import map, which the rule forbids. Revisit if the functions adopt a deno.json import map. | discovered-during: deno-static-gate build 2026-07-14
- open | nullable stripe customer id | getOrCreateStripeCustomer (supabase/functions/_shared/stripe-client.ts) can return `string | null` because billing_customers.stripe_customer_id is nullable; create-billing-portal-session/index.ts (lines ~53, ~78) casts customerId `as string` to pass Stripe. If a row ever holds a null id, Stripe rejects it at runtime (current behavior preserved, not fixed). Consider filtering null ids at the query or throwing a typed error before the Stripe call. | discovered-during: deno-static-gate build 2026-07-14
- open | CI lacks edge-static gate | .github/workflows/ci.yml runs the frontend fast tier + Supabase stack steps directly (not scripts/verify-full.sh), and has no Deno setup, so the new vf_edge_static tier does NOT run in CI. Add a `denoland/setup-deno` step + `npm run verify:edge:static` to ci.yml to make the gate ride along. | discovered-during: deno-static-gate build 2026-07-14
- wontfix | auto-format hook not ported | ai-first-starter's `.claude/hooks/auto-format.ts` PostToolUse hook was intentionally NOT vendored: grant-trail has no prettier config or devDependency, so running it would reformat non-prettier-managed files and create large diff churn. Revisit only if grant-trail adopts prettier. | discovered-during: framework-hooks port 2026-07-14
- open | route-param id types | useGrantBreakdown/useGrantReview receive grant ids as route-param strings but the lib/data layer types them as numbers (PostgREST coerces at runtime, so behavior is correct). The strict-checkJs gate papers over this with documented `/** @type {number} */` casts (hooks/useGrantBreakdown.js, hooks/useGrantReview.js) rather than converting, to stay behavior-preserving. Proper fix: Number() the params at the component boundary and drop the casts. | discovered-during: strict-checkJs gate build 2026-07-14
- open | style drift | W3 (strict-lib) files under frontend/src/lib+hooks were reformatted to prettier defaults (double quotes, 80-col wrap) by a cross-repo auto-format hook, since fixed upstream; those files now differ in quote style from the rest of the repo. Re-align if/when grant-trail adopts a formatter. | discovered-during: strict-lib checkpoint 2026-07-14
- open | inconsistent RLS zero-row guards in lib/data | setExpenseStatus (frontend/src/lib/data/expenses.js:54) and setBudgetItemStatus (frontend/src/lib/data/budgetItems.js:49) throw on a zero-row update ("Update was not applied — check RLS policies for X."), but the other status setters do not: setListingVerification (frontend/src/lib/data/fiscalAgentListings.js:70) and updateInquiryStatus (frontend/src/lib/data/inquiries.js:27) resolve "successfully" when RLS silently drops the update. Suspected intent: the guard should cover all status-mutation paths. Pinned as-is by characterization tests; not normalized during the factory refactor. | discovered-during: lib/data factory refactor 2026-07-14
- open | decline cascade ignores errors | setBudgetItemStatus (frontend/src/lib/data/budgetItems.js:60): when a budget item is declined, the cascade resetting linked expenses to 'pending' awaits the update but never checks its { error } (nor zero-row), so a failed/dropped cascade still reports success to the caller. Suspected intent: cascade failure should surface. Pinned as-is by a characterization test. | discovered-during: lib/data factory refactor 2026-07-14
- open | insert return-shape inconsistency in lib/data | Inserts diverge on whether the created row is returned: insertGrantAttachment/insertReceipt/createTenantSettings/addGrantComment return no row (bare .insert()), insertBudgetItem/insertGrant return .select() arrays, insertExpense/createTenant/invites return .select().single(); payload wrapping ([x] vs x) also varies. Behavior-compatible but easy to misuse. Pinned as-is. | discovered-during: lib/data factory refactor 2026-07-14
- open | dead code (knip report-only) | `npm --prefix frontend run deadcode` reports: 2 unused exports — `hasActiveSubscription` (frontend/src/lib/billing.js:388), `consumeInvite` (frontend/src/lib/invites.js:80); 2 unused devDependencies — `@testing-library/user-event`, `web-vitals` (frontend/package.json); 1 unlisted dep — `csstype` (type-only import in frontend/src/types/css-custom-properties.d.ts, benign). Not deleted this wave (report-only introduction of knip). Remove the exports/deps in a follow-up once confirmed no dynamic use. | discovered-during: boundaries-and-metrics wave 2026-07-14
- open | coverage baseline (report-only) | v8 coverage over frontend src/lib/** + src/hooks/** on 2026-07-14 (275 tests): Lines 74.25% (421/567), Functions 80.89% (144/178), Branches 58.09% (219/377), Statements 74.17% (497/670). No thresholds set this wave; `npm --prefix frontend run coverage` is report-only. A later wave should set the ratchet floor at (or just below) these numbers once stable. | discovered-during: boundaries-and-metrics wave 2026-07-14
- open | null-tenant sync asymmetry | On a legacy user-owned PREMIUM subscription whose payer has no resolvable tenant, the listing publication sync silently no-ops (tenantId 0) but the accepts_sponsorships sync THROWS ('Unable to resolve tenant for sponsorship entitlement sync: no tenant') — so a lapse/reactivate event for such a user fails the whole webhook/sync call after the membership writes succeeded. Pinned as-is (not normalized) by subscription-sync-core.test.ts (planListingPublication vs planSponsorshipEntitlement tenant=0 tests). Decide which behavior is intended and align. | discovered-during: stripe-sync pure-core extraction 2026-07-14
- open | sync shell untestable without DI | stripe-subscription-sync.ts's remaining impure shell (claim RPC ordering guard, billing_customers lookup, upsert sequencing, error wrapping) cannot be unit-tested: stripe-client.ts throws at import without env and binds live Stripe/Supabase clients at module scope (no dependency injection; Deno has no module mocking). Decision logic is now extracted to subscription-sync-core.ts (37 unit tests); the shell is covered only by the Docker/Stripe stack tier. Fix path: inject clients (or a facts-gateway interface) into the shell. | discovered-during: stripe-sync pure-core extraction 2026-07-14
- open | coverage remap warnings | `npm --prefix frontend run coverage` prints two non-fatal `V8CoverageProvider.remapCoverage` stack traces for fully-uncovered files (JSX-in-.js transform has no matching source map), which excludes some untested files from the denominators — so the baseline percentages above may be slightly optimistic. Exit code is 0. Revisit when wiring coverage into a gate. | discovered-during: boundaries-and-metrics wave 2026-07-14
