# DEBT.md — bug & limitation ledger

Any bug or limitation discovered but not fixed in a task gets an entry here, in
the **same commit** as the task. Entries are a running history: don't delete
them — flip `status` to `fixed` or `wontfix` instead.

**Format** (one entry per bug):

```
status | area | description | discovered-during: <context> (date)
```

`status` is one of `open` | `fixed` | `wontfix`.

## Entries

- open | frontend coupling | 8 component files import supabaseClient directly (survey estimated 14; truthful grep 2026-07-14 found 8, now pinned by the shrink-only eslint allowlist in frontend/.eslintrc.cjs) | discovered-during: framework survey 2026-07-14
- open | validation divergence | return-path/origin validation exists only in supabase/functions/_shared/validation.ts with no frontend counterpart; frontend and edge functions can drift on what's accepted | discovered-during: framework survey 2026-07-14
- open | edge lint suppressed | `no-import-prefix` deno lint rule disabled in supabase/functions/deno.json — the edge functions import deps via inline `npm:` specifiers (Supabase Edge runtime pattern) with no import map, which the rule forbids. Revisit if the functions adopt a deno.json import map. | discovered-during: deno-static-gate build 2026-07-14
- fixed | nullable stripe customer id | The `as string` casts in create-billing-portal-session masked an untyped-client inference gap, not a real null path: getOrCreateStripeCustomer only returns a stored id when truthy (a NULL stripe_customer_id falls through to creating a fresh Stripe customer), so null can never reach Stripe. Fixed 2026-07-14: explicit `Promise<string>` return annotations on getOrCreateStripeCustomer / getOrCreateStripeCustomerForTenant make the guarantee visible to the type system; both casts removed (deno check fails without the annotations, so they are load-bearing); verify:edge:static green (37 tests). | discovered-during: deno-static-gate build 2026-07-14
- fixed | CI lacks edge-static gate | .github/workflows/ci.yml had no Deno setup, so the vf_edge_static tier did not run in CI. Fixed 2026-07-14: new `edge-static` job (denoland/setup-deno@v2, deno v2.x, `npm run verify:edge:static`) — fast, no Docker, gates every push/PR. | discovered-during: deno-static-gate build 2026-07-14
- wontfix | auto-format hook not ported | ai-first-starter's `.claude/hooks/auto-format.ts` PostToolUse hook was intentionally NOT vendored: grant-trail has no prettier config or devDependency, so running it would reformat non-prettier-managed files and create large diff churn. Revisit only if grant-trail adopts prettier. | discovered-during: framework-hooks port 2026-07-14
- fixed | route-param id types | useGrantBreakdown/useGrantReview received grant ids as route-param strings papered over with `/** @type {number} */` casts. Fixed 2026-07-14: GrantBreakdown.js and AdminGrantReview.js Number() the param once at the useParams boundary; all casts and stray parseInt conversions removed from the hooks; typecheck:strict green. | discovered-during: strict-checkJs gate build 2026-07-14
- open | style drift | W3 (strict-lib) files under frontend/src/lib+hooks were reformatted to prettier defaults (double quotes, 80-col wrap) by a cross-repo auto-format hook, since fixed upstream; those files now differ in quote style from the rest of the repo. Re-align if/when grant-trail adopts a formatter. | discovered-during: strict-lib checkpoint 2026-07-14
- fixed | inconsistent RLS zero-row guards in lib/data | setListingVerification and updateInquiryStatus resolved "successfully" when RLS silently dropped the update, unlike the throwing set*Status paths. Fixed 2026-07-14: both now carry the same zero-row guard (inquiries via the shared setStatus factory; listings inline since it updates two columns); callers AdminListingVerification.js and FiscalAgentOwnerDashboard.js updated to the throwing contract; characterization tests replaced with guarded-behavior tests. | discovered-during: lib/data factory refactor 2026-07-14
- fixed | decline cascade ignores errors | setBudgetItemStatus: the decline cascade resetting linked expenses to 'pending' never checked its { error }, so a failed cascade still reported success. Fixed 2026-07-14: the cascade now throws on a returned error (zero rows stays fine — a declined item may have no linked expenses); pinning test replaced with cascade-error-throws + zero-row-ok tests. | discovered-during: lib/data factory refactor 2026-07-14
- open | insert return-shape inconsistency in lib/data | Inserts diverge on whether the created row is returned: insertGrantAttachment/insertReceipt/createTenantSettings/addGrantComment return no row (bare .insert()), insertBudgetItem/insertGrant return .select() arrays, insertExpense/createTenant/invites return .select().single(); payload wrapping ([x] vs x) also varies. Behavior-compatible but easy to misuse. Pinned as-is. | discovered-during: lib/data factory refactor 2026-07-14
- fixed | dead code (knip report-only) | knip reported 2 unused exports — `hasActiveSubscription` (billing.js), `consumeInvite` (invites.js) — and 2 unused packages — `@testing-library/user-event`, `web-vitals` (they were in `dependencies`, not devDependencies as first logged). Fixed 2026-07-14: repo-wide grep confirmed no dynamic/string use; both functions deleted, both deps removed (lockfile regenerated); unit suite green (283 tests). The benign `csstype` unlisted-dep report remains and is accepted. | discovered-during: boundaries-and-metrics wave 2026-07-14
- open | coverage baseline (report-only) | v8 coverage over frontend src/lib/** + src/hooks/** on 2026-07-14 (275 tests): Lines 74.25% (421/567), Functions 80.89% (144/178), Branches 58.09% (219/377), Statements 74.17% (497/670). No thresholds set this wave; `npm --prefix frontend run coverage` is report-only. A later wave should set the ratchet floor at (or just below) these numbers once stable. | discovered-during: boundaries-and-metrics wave 2026-07-14
- fixed | null-tenant sync asymmetry | On a legacy user-owned PREMIUM subscription whose payer has no resolvable tenant, the listing publication sync silently no-oped (tenantId 0) but the accepts_sponsorships sync THREW — so a lapse/reactivate event for such a user failed the whole webhook/sync call after the membership writes succeeded. Fixed 2026-07-14: planSponsorshipEntitlement now returns null on an unresolved tenant, aligned with planListingPublication; pinning test replaced with a no-op grid test (subscription-sync-core.test.ts). | discovered-during: stripe-sync pure-core extraction 2026-07-14
- open | sync shell untestable without DI | stripe-subscription-sync.ts's remaining impure shell (claim RPC ordering guard, billing_customers lookup, upsert sequencing, error wrapping) cannot be unit-tested: stripe-client.ts throws at import without env and binds live Stripe/Supabase clients at module scope (no dependency injection; Deno has no module mocking). Decision logic is now extracted to subscription-sync-core.ts (37 unit tests); the shell is covered only by the Docker/Stripe stack tier. Fix path: inject clients (or a facts-gateway interface) into the shell. | discovered-during: stripe-sync pure-core extraction 2026-07-14
- open | coverage remap warnings | `npm --prefix frontend run coverage` prints two non-fatal `V8CoverageProvider.remapCoverage` stack traces for fully-uncovered files (JSX-in-.js transform has no matching source map), which excludes some untested files from the denominators — so the baseline percentages above may be slightly optimistic. Exit code is 0. Revisit when wiring coverage into a gate. | discovered-during: boundaries-and-metrics wave 2026-07-14
- open | verify-full boot kill is shallow | scripts/verify-full.sh kills the background stack-boot subshell on fast-tier failure, but not its `npx supabase start` child (no setsid/process-group kill) — the boot may complete orphaned; harmless (a running stack is inert) but the exit isn't perfectly clean. Also the mktemp boot log is left behind on interrupt (kept deliberately on boot failure). | discovered-during: test-infra speedup 2026-07-19
- open | VF_REUSE_STACK health check is shallow | verify-lib.sh treats a successful `supabase status` as "stack healthy" when VF_REUSE_STACK=1; a partially-up stack that still answers status would skip `supabase start`. The unconditional `db reset` mitigates most of it. | discovered-during: test-infra speedup 2026-07-19
- open | e2e parallel hazards | (1) super-admin-flows.spec.js 'saves platform defaults' mutates the shared platform_settings singleton (id=1) and never restores it — the one parallel-unsafe test now that local Playwright runs 2-4 workers (CI already ran 2, so exposure is unchanged); (2) fixtures' createSubscription (`cus_${Date.now()}`/`sub_${Date.now()}`) and createManagedTenant slug have no random component — same-millisecond unique-constraint collision possible across workers. | discovered-during: test-infra speedup 2026-07-19
- fixed | wait_for_email poll convention | the resend-mock work (lib/resend_mock.py + wait_for_email in stripe_test_helpers.sh) should adopt the 0.5s poll interval wait_for_sql now uses when it lands. Fixed 2026-07-19: the resend-mock work was merged into the speedup branch same-day; wait_for_email polls at 0.5s and uses the shared skip() helper, and its dunning assertion sits inside the LANEF_INCLUDE_SLOW gate so default local runs (which skip the past_due scenario) don't assert on an email that was never sent. | discovered-during: test-infra speedup 2026-07-19
- fixed | broad pkill in edge-test teardown | pre-existing, now documented: email-resilience's stop_serve pkills ANY `functions serve` (including a developer's own shell serve) and run-all.sh's stop_forwarder `pkill -f "stripe listen --api-key"` can hit forwarders of other repos/sessions on the machine. Fixed 2026-07-19 (per-worktree stacks wave): test-infra serves are started with project-tagged env-file names (`lanef-env-<project_id>.*` / `emailres-env[12]-<project_id>.*`) and both teardowns pkill only that pattern; run-all's forwarder sweep now reaps only the pid recorded in this stack's pidfile (`/tmp/lanef-listen-<project_id>.pid`) after re-verifying its argv, so dev.sh's forwarder and other worktrees' processes are never touched. | discovered-during: test-infra speedup 2026-07-19
- open | live-transport double-delivery with a dev forwarder | residual of the forwarder-sweep fix: the old blind sweep incidentally killed a dev.sh `stripe listen` before the live-transport webhook matrix; now it survives, so running `LANEF_WEBHOOK_TRANSPORT=live` on a stack that also has a dev-session forwarder double-delivers every event. Live transport is CI-only by convention (no dev.sh there); locally, stop `npm run dev` before a live-transport run. | discovered-during: per-worktree stacks 2026-07-19
- open | dev.sh is main-checkout-only | `npm run dev` (scripts/dev.sh) still hardcodes API 54321 / Vite 3000 and probes for ANY `supabase_db_` container, so from a linked worktree it targets the MAIN checkout's stack rather than the worktree's own. Fine for its purpose (human dev loop on the main checkout); thread stack-env through it if worktree dev sessions are ever wanted. | discovered-during: per-worktree stacks 2026-07-19
- open | worktree port-slot collision after registry loss | stack-env.sh picks a worktree's port prefix by path-hash and probes /tmp/grant-trail-stacks to avoid live collisions, but the registry lives in /tmp: after a reboot/tmp-clean, two worktrees whose hashes land on the same slot could both re-derive it and collide on `supabase start` (bind error — rerun one after the other finishes booting, or remove one worktree). ~1% chance per worktree pair; deterministic hashing makes it stable, not random. | discovered-during: per-worktree stacks 2026-07-19
- open | shared Stripe TEST account event-window flake | verified 2026-07-19 (wrinkle B): no payment-flow assertion scans account-wide state — DB asserts are keyed to run-created users/subscriptions and Stripe lookups go by object id; the one account-wide read, webhook-matrix's `stripe events list`, is now type-filtered and 100 deep. Residual: enough simultaneous runs could still push a run's event past the 100-window (lookup returns empty → that scenario fails); bump the limit or key by `created[gte]` if it ever bites. | discovered-during: per-worktree stacks 2026-07-19
- fixed | stale e2e spec | cross-role-visibility.spec.js:311 'admin rejects a grant -> grantee sees Rejected' still drove the pre-rename UI (reject button/Rejected text) after the rejected->declined rename (c475125); failed in verify:full e2e tier (31 passed / 1 failed / 3 skipped in its serial group). Fixed 2026-07-14: spec title/selectors/assertions updated to the declined wording (decline buttons, 'Grant declined.', Declined badge/timeline, 'has been declined' notification); re-run against the live local stack: 1 passed. Follow-up same day: CI showed test #5 (budget-item decline cascade) was stale too (admin-reject-btn, status-icon-rejected, 'Budget Item Rejected'); fixed against live UI selectors, full spec file re-run: 7 passed; all other e2e specs swept clean. | discovered-during: final verify:full gate 2026-07-14
- open | platform-root hardcode in promote_admin | scripts/promote_admin.js:95 filters `WHERE t.slug = 'tfac'`, violating the "never hardcode the platform-root tenant slug" invariant (CLAUDE.md); the DB already exposes public.platform_root_slug(). Same file: the catch at :126-128 prints the error but never sets process.exitCode, so a FAILED promotion exits 0 and passes any status check. | discovered-during: scripts necessity audit 2026-07-19
- open | scope.ts module-map lookup is dead | scripts/scope.ts:32 resolves module-map.json at the repo root, but the file lives at frontend/module-map.json. Every argument silently falls through to the literal-path fallback, so the deterministic module-name resolution (the script's headline feature) never fires. The comment at :38-41 claiming "grant-trail has no module-map.json yet" is stale. One-line path fix. | discovered-during: scripts necessity audit 2026-07-19
- fixed | CI does not run the two most security-critical SQL suites | .github/workflows/ci.yml reimplemented the verify tiers as inline steps instead of calling scripts/verify-lib.sh, and named only grant-trigger-behaviors.test.sh and charity-directory-rls.test.sh. vf_sql globs supabase/tests/*.test.sh, so rls-adversarial.test.sh and platform-root-config.test.sh ran locally but NEVER in CI — on a repo whose stated enforcement boundary is RLS. Fixed 2026-07-21: the db-trigger-tests job now runs `bash scripts/verify-lib.sh sql` (the glob is the single source of truth; all four suites — 83+16+81+9 checks — gate every PR/push), and the build-and-test e2e step routes through the root `npm run e2e` (scripts/e2e.sh) instead of an inline credential-extraction recipe. verify-lib gained vf_supabase (prefers CI's pinned PATH CLI from setup-cli, falls back to `npx --prefix frontend supabase` locally) so the same script serves both environments. Verified locally: `bash scripts/verify-lib.sh sql` exit 0 with all four suites green; both CLI-resolution branches exercised. CLAUDE.md's CI-parity claim now holds (edge_identity runs in CI inside run-all.sh). | discovered-during: scripts necessity audit 2026-07-19
- open | managed grantee can set arbitrary grant status via raw API | The "Users can update their own grants" RLS policy (grant_record UPDATE) has no WITH CHECK clause and there is no status-transition trigger, so a grantee can `UPDATE grant_record SET status='approved'` on their own grant through a direct PostgREST call — self-approving past the admin review workflow. The managed-tenant UI never exposes this (CreateGrant only submits 'pending' / resets to 'pending' on edit), so it is UI-invisible but RLS-permitted. For self_service tenants this is now intended (grantee owns the lifecycle; see 20260721013000_self_service_grantee_sets_grant_status). Pre-existing; surfaced while making self-service status grantee-controlled. Fix path: add a WITH CHECK / BEFORE UPDATE guard that, for managed tenants, only lets non-admins move status within grantee-legal transitions (e.g. needs_changes -> pending). | discovered-during: self-service grantee grant status 2026-07-20
- fixed | standalone verify:e2e lacks stack env export | `npm run verify:e2e` / `npm run e2e` were just `stack-lock.sh run npm run e2e --prefix frontend` — unlike verify-full, they never called verify-lib's vf_export_stack_env, so without SUPABASE_SERVICE_ROLE_KEY / VITE_SUPABASE_URL / VITE_SUPABASE_KEY already exported every spec failed in beforeAll with "supabaseKey is required" (deliberately not in .env.local, which holds only browser-safe values). Fixed 2026-07-21: `verify:e2e` now routes through `bash scripts/verify-lib.sh e2e` (lock + boot + db reset + env export — the pre-merge gate; the dispatcher already accepted the e2e tier) and `e2e` through the new `scripts/e2e.sh` (same lock + env export, NO boot/reset, args passed through to Playwright — the fast iteration path, errors out if the stack isn't running). Both verified green with the three vars scrubbed from the caller's env: e2e §5+§6 passthrough 2 passed; verify:e2e full gate 36 passed. | discovered-during: grant insights dashboard 2026-07-21
- open | non-reproducible rls-adversarial offboarding failure | During the auth-cascade work, one verify:rls run reported 2 failures ("tenant-owned sub / tenant membership survives its initiator's deletion") with `enforce_subscription_tier_product_match` raising on the subscriptions.user_id SET NULL. Four subsequent controlled runs (baseline and with-migration, standalone and full-suite, each after a clean db:reset) were all 83 passed / 0 failed, so it was NOT caused by the cascade migration. Most likely residue from ad-hoc probing in the same DB, but the suite proved order/state sensitive under some condition and that sensitivity is unexplained. Re-investigate if it recurs in CI. | discovered-during: auth-user-delete-cascade 2026-07-19
- open | stripe-webhook unknown-customer hard failure | 815 identical CRITICAL failures in staging (2026-07-19→21): "No billing customer found for Stripe customer id." thrown at supabase/functions/_shared/stripe-subscription-sync.ts:108 on customer.subscription.created/updated events for Stripe customers with no billing_customers row; the subscriptions table stayed empty for those events. The webhook should handle unknown customers gracefully (skip/park instead of throwing), and/or the flow that creates those Stripe customers must record a billing_customers row first. | discovered-during: fiscal-agent plan signup-flow fix 2026-07-24
- open | staging Resend API key invalid | 24 payment_failed_email_failure errors in staging: Resend rejects the configured API key with 401, so dunning/payment-failed emails are not being delivered. Rotate/set a valid RESEND_API_KEY in the staging edge-function secrets. | discovered-during: fiscal-agent plan signup-flow fix 2026-07-24
- open | no self-serve remediation for wrong-plan provisioning | A user wrongly provisioned as a Basic grantee (e.g. by the now-fixed dropped ?plan=fiscal-agent param) cannot upgrade to the Fiscal Agent plan themselves: SubscriptionPage is manage-only and tier is role-derived, so there is no grantee→fiscal-agent upgrade path in the UI. Affected paid users need manual remediation (role/tenant fix + subscription change). | discovered-during: fiscal-agent plan signup-flow fix 2026-07-24
- open | moderate uuid advisory via exceljs | `npm audit` reports GHSA-w5hq-g745-h8pq (uuid <11.1.1, missing buffer bounds check in v3/v5/v6 when buf is provided) pulled in via exceljs@4.x, which pins an old uuid. No non-breaking fix: `npm audit fix --force` would DOWNGRADE exceljs to 3.4.0. Moderate severity, below the CI gate (`--audit-level=high`), and the vulnerable uuid buf-API path is not used by exceljs's xlsx generation in practice. Revisit when exceljs ships a release depending on uuid >=11.1.1 (or if the advisory is upgraded). | discovered-during: react-router audit bump 2026-07-24
- fixed | vestigial CI forwarder printed an unbound-variable error | ci.yml's stripe-edge-function-tests step started its own `stripe listen --forward-to "${API_URL}/..."` even though run-all.sh has owned the forwarder lifecycle since it landed (run-all.sh:68, which defaults `${API_URL:-http://127.0.0.1:54321}`). API_URL is not in that step's env, so under `set -u` the expansion failed — but only in the `&`-backgrounded child, so the parent shell printed `API_URL: unbound variable` and carried on; STRIPE_LISTEN_PID captured a dead pid and the trailing `kill` was a no-op. Never a functional failure (run 30108946507: transport live, webhook-matrix 69 passed), just a misleading error line in every CI log and a latent hard failure if the `&` were ever removed. Fixed 2026-07-24: the three vestigial lines deleted; the step keeps only `stripe listen --print-secret` (the CLI's device/account webhook secret, which the run-all-started forwarder signs with) and delegates forwarder start/stop to run-all.sh. Adding API_URL to the step env was rejected — it would resurrect a second forwarder and double-deliver every event (see the live-transport double-delivery entry above). | discovered-during: CI webhook-flow walkthrough 2026-07-24
